# compressarr-node

Remote transcoding agent for Compressarr.

Install globally:

```sh
npm install -g compressarr-node
```

Copy the enrollment command from Compressarr's Settings page and run it on the
transcoding host. FFmpeg and ffprobe must be available in `PATH`.
